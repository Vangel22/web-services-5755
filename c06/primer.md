### Homework

### Models

1. Organization schema
   - name is unique and required
   - it contains timestamps ( this creates fields like: createdAt and updatedAt )
   - An organization is created when registering a new user ( line in code or function for creation ) -> 78 line auth.js file is the creation or
     await createOrganization({ name: organizationName });

### Controllers

### Infrastructure
